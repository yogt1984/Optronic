# Detection models

Not in the repository: model weights are large, separately licensed, and
change independently of the code. `tools/get_model.sh` fetches one.

The processor loads whatever is here by path, so swapping models is a
configuration change rather than a rebuild.
